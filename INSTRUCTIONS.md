# INSTRUCTIONS.md — 謎解き数学ミッションアプリ（Coworkエージェント指示書）

## このフォルダについて

謎解き形式の数学問題Webアプリのプロジェクトフォルダです。
現在は小学校高学年・中学1年生向けの問題を収録。将来的には中学2・3年生・高校生まで対象学年を拡張予定。
**作業を始める前に必ずこのファイルと `index.html` を読み込んでください。**

---

## ファイル構成

```
\OneDrive\ドキュメント\Cowork\math-mystery-quiz\
├── INSTRUCTIONS.md   ← このファイル（最初に必ず読むこと）
├── REVIEW_RULES.md   ← 問題作成の品質チェックルール
├── index.html        ← 本番ファイル（GitHub Pagesにデプロイ済み）
└── drafts/           ← 作業中・テスト用ファイル置き場（本番には影響しない）
```

> **重要**: `index.html` を直接編集する前に必ず `drafts/` にコピーして作業すること。
> 本番ファイルへの上書きは、動作確認後にのみ行うこと。

---

## ⚠️ 問題追加作業を始める前に必ず確認すること

新しい問題を追加するよう依頼された場合、作業を始める前に必ずユーザーに以下を確認すること：

1. **対象レベルはどれか？**
   - LEVEL. 小学校高学年
   - LEVEL. 中学校（前）※中学1年生（4月〜3月）向け
   - LEVEL. 中学校（後）※中学2・3年生向け（将来追加予定）
   - その他（高校生など）

2. **該当レベルのidリストに追加してよいか？**（後述のレベル別id管理を参照）

レベルが不明なまま作業を進めないこと。

---

## アプリの技術仕様

### 技術スタック
- **純粋なHTML/CSS/JavaScript**（React・Babel・ビルドツール一切なし）
- 外部依存はGoogleフォント（Noto Sans JP）のみ
- 1ファイル完結・GitHub Pagesにそのままデプロイ可能（ファイル名は `index.html`）

### 画面遷移
```
問題選択画面（select）→ ストーリー画面（intro）→ 問題画面（quiz）→ 結果画面（result）
```

### 状態管理（グローバル変数）
- `phase`：現在の画面（"select" / "intro" / "quiz" / "result"）
- `qIdx`：選択中の問題セットのインデックス
- `cur`：現在のSTEP番号（0始まり）
- `sel`：選択中の選択肢ラベル（"A"〜"D" or null）
- `confirmed`：答えを確定したか
- `answers`：各STEPの正誤の配列（boolean[]）
- `showHint`：ヒント表示中か

### レンダリング
- `render()` → `buildHTML()` → 各フェーズのbuild関数 → `wrap.innerHTML` に書き込み
- **重要**: `render()`では必ず `classList.remove("out")` してから `classList.add("in")` すること（しないと `opacity:0` のまま表示されない）

### esc()関数
HTMLに文字列を埋め込む際は必ず `esc()` を通すこと（`&` `<` `>` をエスケープ）。
`<pre>` タグ内では `\n` がそのまま改行として表示される。

### subtitleの改行
subtitleに `\n` を含めると、カード表示時に `<br>` に変換されて改行される。
タイトルが長い場合は意図した位置に `\n` を入れること。

### 問題データ構造（`const Q = [...]`）
```js
{
  id: string,
  subtitle: string（\nで任意の位置に改行可）,
  icon: string（絵文字）,
  theme: string,
  difficulty: string（★表記・現在は非表示。データとして残すが画面には出力しないこと）,
  story: string（\nで改行）,
  questions: [
    {
      stage: string（"STEP 1"など）,
      label: string,
      question: string（\nで改行）,
      hint: string,
      choices: [ { label:"A"〜"D", value:string, correct:boolean } ],
      explanation: string（\nで改行）
    }
  ]
}
```

### レベル別id管理（buildSelect関数）
問題選択画面はレベルごとにグループ表示される。
新しい問題を追加した場合は、`buildSelect()` 内の該当レベルの `ids` 配列に追加すること。

```js
const levels=[
  {label:"LEVEL. 小学校高学年", ids:["okozukai_shosha"]},
  {label:"LEVEL. 中学校（前）",  ids:["denki","okozukai","pizza","q4"]}
];
```

新しいレベルを追加する場合は、このlevels配列に新しいオブジェクトを追記する。

### 選択肢のCSS（不具合対応済み）
`choice-btn` の `transition` は `border-color .2s, background .2s, color .2s` に限定すること。
`transition:all` にすると初期描画時に選択肢が光って見える不具合が発生するため使用禁止。

---

## デザイン仕様（変更禁止）

- **テーマ**：ダークテーマ（宇宙・謎解き風）
- **背景**：`radial-gradient(ellipse at 20% 20%, #0d2137, #0a0e1a)`
- **アクセント**：シアン `#00d4ff`、オレンジ `#ff6b35`、ゴールド `#ffd700`
- **正解色**：`#22c55e`（グリーン）／ **不正解色**：`#ef4444`（レッド）
- **最大幅**：720px（モバイル対応済み）
- **レベル見出し**：シアン左ボーダー付き（`.level-label` クラス）
- **問題カード内**：罫線（`───` `┌┐└┘│`）は使用禁止。横スクロールが発生するため。

---

## 現在の問題セット（5問）

### LEVEL. 小学校高学年

#### 第1問：📚「おじいちゃんの／秘密のメッセージ」（3STEP）
- テーマ：小数・分数の計算 ／ 登場人物：ハルト
- STEP1：本棚の何段目か（床から1.11111m、1段0.45m → 3段目）
- STEP2：左から何冊目か（12冊、左から1/4と右から1/3の真ん中 → 6冊目）
- STEP3：古い500円札5枚×5.5倍の価値（約14,000円）

### LEVEL. 中学校（前）

#### 第2問：⚡「冬の電気代の謎を解け！」（3STEP）
- テーマ：電気代・単位換算 ／ 登場人物：ケンタ
- STEP1：ゲーム機（40W）×1時間×30日の電力量（答：1,200Wh）
- STEP2：電気カーペット＋エアコンの合計電気代（答：1,620円）
- STEP3：8時間自動オフ機能で寝落ちが何日あったか（答：8日）

#### 第3問：💰「消えたおこづかいの謎／〜イヤホンへの道〜」（3STEP）
- テーマ：文字式・正負の数・不等式 ／ 登場人物：ミサキ
- STEP1：おこづかい500円アップを反映した貯金シミュレーションの式（Y = 1,800 + 2,000 × X）
- STEP2：2か月分の収支から記録にない支出を逆算（答：1,500円＝クレーンゲーム500円×3回）
- STEP3：税込み価格に向けて不等式で何ヶ月後に買えるか（答：3ヶ月後）

#### 第4問：🍕「ピザはどっちがお得？／図形の罠」（3STEP）
- テーマ：円・おうぎ形・体積 ／ 登場人物：リク
- STEP1〜3：M/Lサイズの面積・単価比較・1切れの体積

#### 第5問：📱「どっちのプランがお得？／スマホ料金のからくり」（3STEP）
- テーマ：比例・料金プラン比較 ／ 登場人物：ハルカ
- STEP1〜3：画質別データ使用量の比較・プランAの月額計算・3プランの最安値比較

---

## 問題追加の手順（新しい問題を頼まれたとき）

1. **対象レベルをユーザーに確認する**（必須・作業前に行うこと）
2. `index.html` を `drafts/draft_YYYYMMDD.html` にコピーする
3. `const Q` 配列に新しいオブジェクトを追記する（既存の構造に厳密に従うこと）
4. `buildSelect()` 内の該当レベルの `ids` 配列に新しいidを追加する
5. **数値の整合性を必ず検算すること**（問題文・選択肢・解説の数値が一致しているか）
6. 問題文に答えのヒントを書きすぎないこと（特に最終STEPの謎解き部分）
7. 動作確認後、`index.html` に反映する
8. GitHubにコミット・プッシュする（コミットメッセージは内容を端的に示すこと）

### 問題追加の候補テーマ（優先順）

**小学校高学年**
1. 割合・百分率（買い物・セール・消費税）
2. 速さ・時間・距離
3. 図形の面積・体積
4. 整数・分数・小数の複合計算

**中学校（前）**
1. データの活用（平均・最頻値・中央値）
2. 文字式の利用（料金・個数の文字式）
3. 空間図形（展開図・表面積）

**中学校（後）**
1. 連立方程式（料金・個数の比較）
2. 一次関数（グラフ・変化の割合）
3. 確率（日常場面のくじ・ゲーム）

---

## 今後の機能追加候補

- 正解数に応じたバッジ・称号の表示
- 問題ごとの既クリアフラグ保存（localStorage）
- LEVEL. 中学校（後）の追加（中学2・3年生向け）
- BGMや効果音の追加

---

## 注意事項（必ず守ること）

- 問題文・解説の数値は全て検算済み。変更する場合は必ず整合性を確認すること
- `index.html` への直接上書きは確認を取ってから行うこと
- GitHub Pagesへのデプロイ時はファイル名が `index.html` であることを確認すること
- デザイン仕様（色・フォント・レイアウト）は勝手に変更しないこと
- 問題カード・ストーリー・問題文に罫線（`───` `┌┐└┘│`）を使用しないこと
- `choice-btn` の `transition:all` は使用禁止（初期描画の不具合が発生する）
